import { ScrollView } from 'react-native';

import { Text } from '@td-design/react-native';

export function Agreement() {
  return (
    <ScrollView>
      <Text>请根据项目情况自行补充</Text>
    </ScrollView>
  );
}
